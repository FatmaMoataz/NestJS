export enum ProviderEnum {
  SYSTEM = 'system',
  GOOGLE = 'google',
}

export enum RoleEnum {
  admin = 'admin',
  superAdmin = 'super_admin',
  user = 'user',
}

export enum GenderEnum {
  male = 'male',
  female = 'female',
}
